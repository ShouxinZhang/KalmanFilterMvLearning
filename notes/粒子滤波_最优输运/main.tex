\documentclass[12pt,a4paper]{ctexart}
\input{preamble.tex}

\title{粒子滤波与最优输运笔记\\
\large 从重采样到运输映射}
\author{}
\date{\today}

\begin{document}

\maketitle
\tableofcontents

\paragraph{定位}
这份笔记默认你已经知道基础粒子滤波。这里不重写一整篇 PF，只回顾理解最优输运更新所必需的最短背景；不讨论神经网络，也不讨论三者同时混合。

\section{为什么要把最优输运接到粒子滤波上}

基础粒子滤波在加权之后得到的是带权经验分布
\[
\mu^w = \sum_{i=1}^N w_i \delta_{x_i^f},
\qquad
\sum_{i=1}^N w_i = 1.
\]
经典重采样把它重新变成等权样本
\[
\mu^{\mathrm{rs}} = \frac{1}{N}\sum_{j=1}^N \delta_{\tilde x_j}.
\]

问题在于，随机重采样虽然简单，但往往通过复制少数高权粒子完成“去权重化”，因此会带来样本贫化和额外的 Monte Carlo 方差。最优输运方法的目标，是把“去权重化”看成一个运输问题：不再随机复制，而是以最小代价把带权粒子云变成等权粒子云。

\section{把重采样重新表述成一个运输问题}

\subsection{离散耦合矩阵}

设预测粒子为 \(\{x_i^f\}_{i=1}^N\)，对应归一化权重 \(\{w_i\}_{i=1}^N\)。我们希望构造分析粒子 \(\{x_j^a\}_{j=1}^N\)，且每个分析粒子都带相同质量 \(1/N\)。

在离散情形下，引入耦合矩阵
\[
P = (p_{ij}) \in \R^{N \times N}_{\ge 0},
\]
并要求
\[
\sum_{j=1}^N p_{ij} = w_i,
\qquad
\sum_{i=1}^N p_{ij} = \frac{1}{N}.
\]
第一组约束表示第 \(i\) 个预测粒子的质量总共被分配了 \(w_i\)；第二组约束表示每个分析粒子接收的总质量都是 \(1/N\)。

\subsection{最优输运目标}

若代价函数取平方距离
\[
c_{ij} = \|x_i^f - x_j^f\|^2,
\]
则离散最优输运问题写成
\[
P^\star
=
\argmin_{P \ge 0}
\sum_{i=1}^N \sum_{j=1}^N p_{ij} c_{ij}
\]
满足上面的行和、列和约束。

这条式子的含义很直接：把带权经验分布 \(\mu^w\) 变成等权经验分布时，希望“总搬运路程”尽量短。与随机重采样相比，它给出的是一个结构化的搬运方案。

\section{从耦合矩阵到新的等权粒子}

\subsection{ETPF 的线性变换}

在 Ensemble Transform Particle Filter（ETPF）中，分析粒子由
\[
x_j^a = N \sum_{i=1}^N p_{ij}^\star x_i^f
\]
给出。由于列和满足 \(\sum_i p_{ij}^\star = 1/N\)，所以上式是一个凸组合，因此每个 \(x_j^a\) 都位于原粒子凸包之内。

\subsection{均值守恒}

这个构造有一个重要性质：分析样本均值等于带权预测均值。证明很短：
\[
\frac1N \sum_{j=1}^N x_j^a
=
\frac1N \sum_{j=1}^N \left(
N \sum_{i=1}^N p_{ij}^\star x_i^f
\right)
=
\sum_{i=1}^N x_i^f \sum_{j=1}^N p_{ij}^\star
=
\sum_{i=1}^N w_i x_i^f.
\]
因此 OT 更新不是随意平滑，而是在保持一阶统计量的同时，把带权表示改写成等权表示。

\subsection{与随机重采样的本质差别}

随机重采样产生的是祖先索引，因此输出样本通常是原粒子的重复拷贝。OT 更新输出的则是原粒子的线性组合，因此更平滑，也通常减少额外采样噪声。

\section{一维离散算例：普通重采样与运输式更新}

\subsection{预测粒子与权重}

设一维预测粒子为
\[
x^f = (0,\ 1,\ 3),
\]
权重为
\[
w = (0.1,\ 0.3,\ 0.6).
\]
其带权均值为
\[
\sum_{i=1}^3 w_i x_i^f = 0.1 \cdot 0 + 0.3 \cdot 1 + 0.6 \cdot 3 = 2.1.
\]

\subsection{一次随机重采样的结果}

若做一次多项式重采样，完全可能抽到祖先索引
\[
(2,\ 3,\ 3),
\]
于是得到等权粒子
\[
\tilde x = (1,\ 3,\ 3).
\]
这组样本的均值为
\[
\frac{1 + 3 + 3}{3} = \frac73 \approx 2.33,
\]
与原带权均值 \(2.1\) 已出现偏差。这种偏差不是模型信息，而是重采样本身带来的随机扰动。

\subsection{构造一个可行运输计划}

现在改用 OT 更新。考虑耦合矩阵
\[
P =
\begin{bmatrix}
0.1 & 0   & 0 \\
0.2333 & 0.0667 & 0 \\
0 & 0.2666 & 0.3334
\end{bmatrix}.
\]
它近似满足行和
\[
(0.1,\ 0.3,\ 0.6)
\]
和列和
\[
\left(\frac13,\ \frac13,\ \frac13\right).
\]

\subsection{计算分析粒子}

按照
\[
x_j^a = 3 \sum_{i=1}^3 p_{ij} x_i^f
\]
得到
\[
x_1^a = 3(0.1 \cdot 0 + 0.2333 \cdot 1 + 0 \cdot 3) \approx 0.70,
\]
\[
x_2^a = 3(0 \cdot 0 + 0.0667 \cdot 1 + 0.2666 \cdot 3) \approx 2.60,
\]
\[
x_3^a = 3(0 \cdot 0 + 0 \cdot 1 + 0.3334 \cdot 3) \approx 3.00.
\]
于是分析粒子为
\[
x^a \approx (0.70,\ 2.60,\ 3.00).
\]
它们的等权均值是
\[
\frac{0.70 + 2.60 + 3.00}{3} = 2.10,
\]
与原带权均值完全一致。

\subsection{这个算例说明了什么}

普通重采样产生的是重复粒子 \((1,3,3)\)，而 OT 更新给出的是平滑搬运后的粒子 \((0.70, 2.60, 3.00)\)。后者没有凭空丢掉低权粒子携带的信息，而是通过耦合矩阵把这部分质量有组织地搬到了新的等权样本中。

\section{与粒子滤波更新的关系}

\subsection{不是替代似然，而是替代重采样}

最优输运方法并没有改掉粒子滤波的贝叶斯更新本质。似然仍然通过权重
\[
w_i \propto p(y_t \mid x_i^f)
\]
进入算法。改变的是“如何从带权粒子变回等权粒子”。

\subsection{一个更精确的理解}

因此 OT 类方法的逻辑是：
\begin{enumerate}
  \item 预测阶段仍按粒子滤波生成 \(x_i^f\)；
  \item 更新阶段先按似然得到权重 \(w_i\)；
  \item 然后不做随机复制，而是求运输计划 \(P^\star\)；
  \item 最后由 \(P^\star\) 构造新的等权分析粒子 \(x_j^a\)。
\end{enumerate}

\section{计算代价与正则化}

离散 OT 的主要障碍是计算量。样本数大时，直接求解线性规划会变重。常见做法是加熵正则：
\[
\min_{P \ge 0}
\sum_{i,j} p_{ij} c_{ij}
\;+\;
\varepsilon \sum_{i,j} p_{ij}(\log p_{ij} - 1),
\]
并继续满足行和、列和约束。这样可以用 Sinkhorn 型迭代加速求解。

但要看清代价：你减少了重采样方差，却引入了一个新的优化问题。因此 OT 方法通常适合样本质量比样本数量更关键的场景，而不是任何 PF 都无脑套上去。

\section{总结}

粒子滤波与最优输运的组合，可以理解成对“重采样”这一步的精细化改造：
\begin{enumerate}
  \item PF 负责产生带权经验分布；
  \item OT 负责把带权分布平滑地变成等权分布；
  \item ETPF 则把这一过程写成耦合矩阵与线性变换。
\end{enumerate}

如果只记一句话，那么应当是：
\begin{quote}
最优输运不是在改变粒子滤波的贝叶斯逻辑，而是在减少“从权重回到样本”这一步的随机损失。
\end{quote}

\end{document}
